---
type: サブスクリプション-index
作成日: 2026-06-06
---

# サブスク集計

## 月額換算一覧

```dataviewjs
const pages = dv.pages('"11_サブスクリプション"')
  .where(p => p.type === "サブスクリプション")
  .sort(p => p.月額換算, 'desc');

const yen = n => "¥" + Number(n ?? 0).toLocaleString("ja-JP");

dv.table(
  ["File", "プラン", "カテゴリ", "支払方法", "月額換算", "年額換算", "契約状況"],
  pages.map(p => [
    p.file.link,
    p.プラン,
    p.カテゴリ,
    p.支払方法,
    yen(p.月額換算),
    yen(p.年額換算),
    p.契約状況
  ])
);
```

## 月額合計

```dataviewjs  
const pages = dv.pages('"11_サブスクリプション"')  
.where(p => p.type === "サブスクリプション" && p.契約状況 === "active")  
.array();  
  
const total = pages.reduce((sum, p) => sum + Number(p.月額換算 ?? 0), 0);  
  
dv.table(  
["項目", "金額"],  
[["月額合計", "¥" + Math.round(total).toLocaleString("ja-JP")]]  
);  
```

## 年額合計

```dataviewjs  
const pages = dv.pages('"11_サブスクリプション"')  
.where(p => p.type === "サブスクリプション" && p.契約状況 === "active")  
.array();  
  
const total = pages.reduce((sum, p) => sum + Number(p.年額換算 ?? 0), 0);  
  
dv.table(  
["項目", "金額"],  
[["年額合計", "¥" + Math.round(total).toLocaleString("ja-JP")]]  
);  
```

## カテゴリ別合計

```dataviewjs
const pages = dv.pages('"11_サブスクリプション"')
  .where(p => p.type === "サブスクリプション" && p.契約状況 === "active");

const categories = {};

for (const p of pages) {
  const cat = p.カテゴリ ?? "未分類";

  if (!categories[cat]) {
    categories[cat] = {
      monthly: 0,
      yearly: 0
    };
  }

  categories[cat].monthly += p.月額換算 ?? 0;
  categories[cat].yearly += p.年額換算 ?? 0;
}

const rows = Object.entries(categories)
  .map(([cat, data]) => [
    cat,
    "¥" + data.monthly.toLocaleString("ja-JP"),
    "¥" + data.yearly.toLocaleString("ja-JP"),
    data.monthly
  ])
  .sort((a, b) => b[3] - a[3]);

dv.table(
  ["カテゴリ", "月額合計", "年額合計"],
  rows.map(r => [r[0], r[1], r[2]])
);
```
## 注意

- ChatGPT Plusは日本円固定ではなく、実請求額が為替で変動します。
- Adobe Creative Cloudは実際の年一括支払い額、特にAmazonセール購入額と異なる可能性があります。
- 実請求額が判明したら、各 `.md` ファイルの `月額換算` と `年額換算` を修正。
