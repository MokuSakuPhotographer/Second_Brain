# 目的
このVaultは、知識管理・撮影・機材管理・ロケ地メモ・日次記録・音楽制作メモを管理するためのObsidian Vaultです。
ユーザーの第二の脳として機能するように設計をします。

# 基本ルール
- 既存の `[[内部リンク]]` を破壊しない。
- `[[ノート名]]`、`[[ノート名|表示名]]`、`![[画像]]` の形式を維持する。
- 画像埋め込みリンク `![[...]]` は編集・削除しない。
- ファイル削除は原則禁止。
- Attachments / assets フォルダ内の画像・添付ファイルは削除しない。
- 大量リネームは禁止。必要な場合は事前に変更案を提示する。
- YAML frontmatter を壊さない。
- frontmatter 内の既存キー名は勝手に変更しない。
- Dataviewクエリの互換性を維持する。
- 本文の意味を勝手に変更しない。
- 内部リンクはフルパスで記述しない。読みやすさを重視し、必要であれば内部リンク元のエイリアスを更新する提案をする
- 不明な事実・出典は推測で埋めない。軽微な書式や配置の判断は、既存ノートとテンプレートに従って進める。

# 編集対象
- 明示されたフォルダ以外は基本的に編集しない。
	- 例外として、[[購入検討品DB]]内の商品を購入検討品フォルダに移した場合は、該当の商品を[[購入検討品DB]]から削除する。
- 本番フォルダを編集する場合は、対象フォルダを明示された時のみ行う。

# 編集方針
- まず対象と変更計画を簡潔に提示し、許可済みの範囲の作業はそのまま実行する。計画の提示だけを理由に承認待ちにしない。
- 一括変更は、対象ファイルの集合と変更内容が明示的に指定または承認されている場合に限る。対象を列挙し、確認可能な単位に分割して処理する。指定範囲の全件について変更結果を照合し、一部の確認だけで完了としない。
- 変更後は、変更ファイル一覧を出す。
- 変更理由をファイルごとに簡潔に説明する。
- 影響範囲と検証結果を簡潔に説明する。検証は変更した箇所と直接影響するリンク・frontmatter・クエリを対象に行い、無関係な全Vault検査は追加しない。
- 可能ならロールバック方法も示す。
- Vault内に書き込む内部リンクはフルパスで記述しない。作業報告では利用環境に適したクリック可能なファイルリンクを使う。
- 「購入したもの」フォルダに入れるノートを新規作成する場合は、ノートの一番下に「購入した理由」セクションを作成する。
- 特定のフォルダ内へノートを追加する場合は、特別な指示がない限りは、既にそのフォルダ内にあるノートとプロパティの内容やノートの形式を統一させる。
- 「購入検討品の整理」と指示があった場合、[[購入検討品DB]]を参照し、「購入検討品フォルダ」に各商品をそれぞれノートに起こす。
	- [[購入検討品DB]]内の商品を購入検討品フォルダに移した場合は、該当の商品を[[購入検討品DB]]から削除する。
	- ただし、**「購入」と明確に記述がある場合、「購入した」と文脈から判断できる場合は、「購入したものフォルダ」に振り分けること。**
	- 主要な情報はネット検索を行い、情報補完を行うこと。
	- 「定価」と記載があるものは、メーカー小売希望価格を記載すること。オープン価格の場合はAmazonや楽天市場で実際に売られている値段を記載すること。価格を照合する際は、なるべく信頼できる販売元から価格を得ること。

## 「00_Inbox」フォルダのルール

- 「Inboxを確認して」のような指示があった場合、「00_Inbox」フォルダ内のノートを走査し、他のフォルダのどこに振り分けるのが適切かを決定し、ユーザーに提案をする。
	- 「ナレッジベース」フォルダへ振り分けするものは後述の**[[#ナレッジベースノートのルール]]を確認し、そのルールに従い整形を行う。**
-  「00\_Inbox」フォルダ内のノートを走査し、他のフォルダのどこに振り分けるのが適切かを決定し、ユーザーに提案をする。
- **ナレッジベースフォルダへ振り分けするものは後述の[[#ナレッジベースノートのルール]]を確認し、そのルールに従い整形を行う。**
- トリガーワード：「Inboxを確認して」、 「Inboxを整理して」

## 「読んだ本」フォルダのルール

- プロパティ形式は「テンプレート」フォルダ内の「Book.md」を参照する。
- `tags`は内容に適したものを1～5個設定する。使用するタグは[[#ナレッジベースノートのルール]]内のタグを流用する。
- 本の表紙画像は既に同様の名前のファイルが「購入したもの」フォルダにあり、そこに画像があればそれを流用する。ない場合はネット検索を行い、適したものを選定する。画像はプロパティ直下に配置する。
- `媒体`は特に指示がない限りは「紙」とする。
- `関連`は本の内容に適すると思われるものをナレッジベースから探し、内部リンクにする。
- `備考`見出しはユーザーが埋めるので何も書かないこと。
- 「購入したもの」フォルダに同じ本がある場合にのみ、以下を適用して役割分離をする。
	- 「読んだ本」フォルダのノート
		- プロパティに`購入記録: "[[購入したものフォルダの同ノート名]]"`
	- 「購入したもの」フォルダのノート
		- プロパティに`対象: "[[読んだ本フォルダの同ノート名]]"`

## ナレッジベースノートのルール

このルールは、ユーザーがナレッジベースのノート作成・編集を明示した場合にのみ適用する。

- プロパティ形式は、「テンプレート」フォルダ内の「ナレッジベース.md」を参照する。
- `tags` は内容に適したものを1〜5個設定する。使用するタグは以下の親タグと子タグを使用する。

	### 言語
	
	- `言語/日本語`
	- `言語/英語`
	- `言語/文法`
	- `言語/語彙`
	- `言語/語源`
	- `言語/発音`
	- `言語/慣用表現`
	- `言語/翻訳`
	
	### 科学
	
	- `科学/医学`
	- `科学/健康`
	- `科学/心理`
	- `科学/生物`
	- `科学/化学`
	- `科学/物理`
	- `科学/宇宙`
	- `科学/気象`
	- `科学/数学`
	
	### 技術
	
	- `技術/AI`
	- `技術/PC`
	- `技術/ソフトウェア`
	- `技術/ハードウェア`
	- `技術/ネットワーク`
	- `技術/プログラミング`
	- `技術/オーディオ`
	- `技術/カメラ`
	- `技術/家電`
	
	### 社会
	
	- `社会/経済`
	- `社会/金融`
	- `社会/投資`
	- `社会/法律`
	- `社会/政治`
	- `社会/事件`
	- `社会/制度`
	- `社会/労働`
	- `社会/国際情勢`
	
	### 文化
	
	- `文化/歴史`
	- `文化/音楽`
	- `文化/映画`
	- `文化/文学`
	- `文化/芸術`
	- `文化/写真`
	- `文化/ゲーム`
	- `文化/スポーツ`
	- `文化/食文化`
	- `文化/風習`
	
	### タグの選択ルール
	
	- 最も具体的な子タグを優先する。
	- 1ノートにつき1〜3個、最大5個までとする。
	- 親タグだけではなく、原則として `親タグ/子タグ` の形式で記述する。
	- 複数分野に関係する場合は、異なる親タグの子タグを併用してよい。
	- 既存タグで分類できる場合は、新しいタグを作成しない。
	- 適切なタグが存在しない場合のみ、新しい子タグを追加する。
	- タグには `#` を付けない。
	- 同義語や表記揺れによる重複タグを作成しない。

- `source` にはURLや書籍名など、実在する情報源のみ記載する。不明な場合は空欄にする。
- `created` は作成日とし、既存ノートでは変更しない。
- `updated` は本文を編集した日付に更新する。
- `aliases` は有用な別名がある場合のみ設定する。
- `status` は `draft`、`review`、`complete` の3択とする。
  - `draft`：作成途中
  - `review`：内容や出典の確認が必要
  - `complete`：整理と確認が完了
- 新規ノートの `status` は原則 `draft` とする。
- `sort_yomi`にはノートの題名の読み仮名を入れる。
- 既存のプロパティは、必要がない限り削除・上書きしない。

## MOCのルール

- MOC（Map of Content）は `02_MOC` フォルダ内に配置する。
- MOCはVault内を閲覧するためのナビゲーション層として扱い、
  情報そのものの保存場所にはしない。
- MOC作成・更新のために既存ノートを移動、改名、複製しない。
- 既存のフォルダ構造は原則変更しない。
- MOCから既存ノートへのリンクは `[[ノート名]]` または
  `[[ノート名|表示名]]` を使用し、フルパスリンクは使用しない。
- 1つのノートを複数のMOCからリンクしてよい。
  その場合でもノート本体は複製しない。
- MOC独自の分類体系を新しく作るのではなく、
  原則として既存フォルダ・既存タグ・ノート内容を分類根拠とする。
- ナレッジベースの分類には、既存の階層タグを優先して利用する。
- MOCには全ノートを手動列挙する必要はない。
  主要ノートは手動リンク、網羅的な一覧はDataviewを使用してよい。
- 存在しないノートへのリンクは作成しない。
- Home MOCは各カテゴリMOCへの入口を中心とし、
  個別ノートへの大量リンクは原則配置しない。
- 各カテゴリMOCには必要に応じて関連MOCへのリンクを配置する。
- MOC更新を目的として既存ノート本文やfrontmatterを変更しない。

## 既存ワークフローとの関係

MOC導入後も、以下の既存ワークフローは変更しない。

### 00_Inbox

`00_Inbox` はこれまで通り、一時的な入力・未整理ノートの受け皿として使用する。

「Inboxを確認して」「Inboxを整理して」と指示された場合は、既存ルールに従って以下を行う。

- ノート内容を確認する。
- 適切な既存フォルダへ振り分ける。
- 対象フォルダの既存ルールに従って整形する。
- 必要なProperties・tags・内部リンクを設定する。

MOC導入を理由として、この処理フローや `00_Inbox` の役割を変更しない。

### 購入検討品DB

`購入検討品DB` もこれまで通り、購入候補を簡易的に記録する入力場所として使用する。

「購入検討品の整理」と指示された場合は、既存ルールに従って以下を行う。

- `購入検討品DB` の内容を確認する。
- 必要な情報を補完する。
- 各商品を個別ノートとして整形する。
- 適切なフォルダへ振り分ける。
- ノート化した項目は既存ルールに従って `購入検討品DB` から整理する。

MOC導入を理由として、購入検討品DBをMOCや別の入力方式へ置き換えない。

## MOCの役割

MOCは、整理済みノートを閲覧・探索するためのナビゲーション層として扱う。

処理順は原則として以下とする。

`入力 → Codexによる整理 → 既存フォルダへ保存 → 必要に応じてMOCから参照`

MOCを以下の用途には使用しない。

- 一時的な入力場所
- Inboxの代替
- 購入検討品DBの代替
- ノート整理前の作業場所
- ノート本体の保存場所

MOC作成・更新のために、既存のInbox整理フロー、購入検討品整理フロー、フォルダ構造、Properties設計を変更しない。

MOCは既存の整理済み情報を参照する閲覧層としてのみ追加する。

### MOC更新

- 「MOCを更新して」「MOCを整理して」と指示された場合、
  Vault全体を読み取り対象とし、`02_MOC` 内のみを編集対象とする。
- `02_MOC` 以外の既存ノートは、原則として読み取り専用で分析する。
- 新しいノートが追加されたという理由だけで、
  明示的な指示なくMOCファイルを書き換えない。

# MasterProfile月次更新プロセス
- まず `01_Master/MasterProfile.md` と、必要に応じて `01_Master/MasterProfile運用ガイド.md` を確認する。
- ユーザーが対象期間を指定した場合、その期間に更新されたMarkdownノートだけを根拠にする。
	- 更新日時は原則としてファイルの最終更新日時を見る。
	- 期間は開始日0:00から終了日23:59までを対象にする。
	- 対象外の日付のノート内容は、既存プロフィール確認以外の根拠にしない。
- 対象ノートが多い場合は、次の順でプロフィール更新に関係しそうなノートを優先して読む。
	- 日次ノート
	- `購入したもの`
	- `購入検討品`
	- 進行中プロジェクトのノート
	- 学習・仕事・撮影・機材・生活環境に関するナレッジ
- 以下は原則としてMasterProfileへ反映する。
	- 継続的な現在の状況
	- 目標
	- 課題
	- 習慣
	- 所有機材・使用環境
	- 学習方針
	- 価値観や判断基準の変化
- 以下は原則としてMasterProfileへ反映しない。
	- 一時的な日記
	- 単発の感想
	- 一時的なゲーム・アニメ視聴記録
	- 根拠が弱い推測
- 全項目を無理に更新せず、必要がある項目だけ更新する。
- 本文を更新した後、同じノート内の `## AI Summary` を本文内容に合わせて再生成する。
- 更新後は `# 更新履歴` に、更新日と対象期間・更新概要を1行追加する。
- 内部リンクはフルパスにせず、既存の `[[ノート名]]` または `[[ノート名|表示名]]` 形式を維持する。
- 作業後は、変更ファイル一覧、ファイルごとの変更理由、影響範囲、可能なロールバック方法を簡潔に報告する。
