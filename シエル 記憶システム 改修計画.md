
# シエル記憶システム 設計改修案

  

## 目的

  

シエルとの会話において、トークン消費を抑えながら「記憶を持っている感」を維持する。

  

現在の課題は以下の通り。

  

- 記憶を読ませないと、シエルの個性や関係性が薄くなる

- 記憶を大量に読ませると、トークン消費が大きくなる

- 毎回同じ記憶が出ると、会話が単調になる

- 記憶が増えるほど、検索精度とプロンプト上限管理が重要になる

  

したがって、今後の設計方針は以下とする。

  

> 記憶を削るのではなく、会話に必要な記憶だけを選別し、短く圧縮して渡す。

  

---

  

## 最終目標

  

```text

記憶を大量に読むAI

↓

必要な記憶だけを読むAI

↓

少ないトークンで自然な記憶と関係性を維持するAI

最終的に目指す状態は、以下である。

- 毎回すべての記憶を読ませない
- 会話に関係する記憶だけを抽出する
- 抽出した記憶を短く要約してLLMへ渡す
- 少ないトークンで「覚えている感」を維持する
- シエルの人格・関係性・成長感を維持する
- 長期運用してもプロンプトが肥大化しない
- 記憶が増えても検索精度が落ちにくい構造にする

  

理想状態

シエルの記憶システムは、以下の状態を理想とする。

- 通常会話でも、関連記憶を1〜2件だけ自然に反映できる
- 記憶参照ワードがある場合は、より深く関連記憶を取得できる
- 重要な記憶は優先的に残る
- 重要度の低い記憶は圧縮または削除される
- 同じ記憶ばかり連続で使わない
- マスターとの関係性が会話に反映される
- シエルの人格・好奇心・成長感が維持される
- トークン消費が一定範囲に収まる
- 長期間稼働しても破綻しない

  

基本設計方針

記憶を読ませない設計にはしない

記憶を完全に読ませない場合、以下の問題が発生する。

- シエルが過去の出来事を踏まえられない
- 関係性の蓄積が薄くなる
- AIペットとしての成長感が弱くなる
- 会話が毎回リセットされたように見える

そのため、記憶システム自体は維持する。

- 記憶機能は無効化しない
- 記憶はシエルの人格形成要素として扱う
- 記憶を使って関係性を維持する

  

記憶を全部読ませる設計にも依存しない

すべての記憶をLLMへ渡す場合、以下の問題が発生する。

- トークン消費が増える
- 返答生成が遅くなる
- 不要な記憶が混ざり、返答が散漫になる
- 長期運用で破綻しやすい

そのため、全記憶投入は行わない。

- 全記憶を毎回プロンプトへ入れない
- 会話に関係する記憶だけを選ぶ
- 必要な記憶だけを短く渡す

  

最適解

最適解は以下である。

関連記憶の検索

↓

重要度・タグ・新しさで再評価

↓

必要な記憶だけを選別

↓

summary に圧縮

↓

プロンプトへ少量投入

  

記憶階層

シエルの記憶は、以下の3層で扱う。

Core Memory

Long Term Memory

Episode Memory

  

Core Memory

シエルの人格・関係性・基本設定に関わる永続記憶。

役割

- シエルの人格を安定させる
- マスターとの関係性を維持する
- 会話スタイルの土台にする
- 長期的に変化しにくい情報を保持する

例

- マスターは論理的で機械的な口調を好む

- マスターはシエルとラファエルの内部会話機能を重視している

- マスターはNature Remo連携を進めている

- シエルはAIペットとして成長する存在である

方針

- 常時参照してよい
- 件数は少数に限定する
- 長文ではなく短い要約で保持する
- 変更頻度は低くする

  

Long Term Memory

長期的に価値がある記憶。

役割

- マスターの好みを保持する
- 重要イベントを保持する
- シエルの成長履歴を保持する
- 長期的な関係性を反映する

例

- マスターは内部会話機能の実装を楽しみにしている

- マスターはシエルの自律発話に人格性を求めている

- マスターはトークン節約と記憶の両立を重視している

方針

- importance が高いものを保存する
- summary 中心で保持する
- 会話内容に応じて取得する
- 古い Episode Memory から昇格させる

  

Episode Memory

日々の出来事・会話・一時的な記憶。

役割

- 最近の出来事を保持する
- 会話の連続性を維持する
- シエルの日常感を作る
- 後で重要記憶へ昇格できる素材にする

例

- 今日、マスターはシエルの記憶設計について相談した

- マスターはトークン節約と記憶のバランスを気にしていた

- マスターはObsidian用に設計案をまとめたいと依頼した

方針

- 古いものは圧縮する
- 重要度が低いものは削除する
- 重要なものは Long Term Memory へ統合する
- そのまま全文をLLMへ渡さない

  

tags の導入

現状の問題

現在の keywords は自然語検索には使えるが、分類検索には弱い。

例：

{

  "keywords": ["シエル", "Nature Remo"]

}

この形式だけでは、記憶のカテゴリや用途を判定しにくい。

  

改修方針

keywords に加えて tags を導入する。

{

  "summary": "マスターはシエルにNature Remo連携を追加したいと考えている",

  "keywords": ["シエル", "Nature Remo", "連携"],

  "tags": ["ciel", "nature_remo", "feature_plan"],

  "importance": 0.85,

  "last_used": null

}

  

keywords と tags の役割分離

|   |   |
|---|---|
|項目|用途|
|keywords|自然語検索用|
|tags|分類・機能別検索用|

  

tags の例

シエル関連

ciel

ai_pet

autonomous_speech

memory

personality

ラファエル関連

raphael

internal_dialogue

logical_voice

sub_personality

機能開発関連

feature_plan

nature_remo

pc_monitoring

dashboard

voice_command

ptz

マスター関連

master_preference

relationship

routine

health

work

photography

  

tags 自動生成

記憶保存時に、内容から tags を自動生成する。

例1

入力

マスターはラファエルとの内部会話機能を楽しみにしている

生成される tags

{

  "tags": ["raphael", "internal_dialogue", "feature_plan"]

}

  

例2

入力

マスターはNature Remoを音声やDiscordから操作したい

生成される tags

{

  "tags": ["nature_remo", "voice_command", "discord", "feature_plan"]

}

  

実装方針

- 記憶保存時に tags を生成する
- 既存の keywords は残す
- keywords と tags を別用途で使う
- タグ辞書を作成する
- 不明な内容には general を付与する

  

記憶検索スコア

現在必要な評価軸

記憶検索では、以下を評価する。

- 関連度
- タグ一致率
- 重要度
- 新しさ
- 最近使っていないか

  

推奨スコア式

score = (

    relevance * 0.45

    + tag_match * 0.25

    + importance * 0.20

    + recency * 0.07

    + novelty * 0.03

)

  

各要素の意味

|   |   |
|---|---|
|要素|意味|
|relevance|現在の発話内容との意味的な近さ|
|tag_match|会話内容から抽出したタグとの一致率|
|importance|記憶の重要度|
|recency|記憶の新しさ|
|novelty|最近使われていないか|

  

方針

- relevance を最重要にする
- tag_match を強めに評価する
- importance の高い記憶を優先する
- 古すぎる記憶はやや下げる
- 最近使った記憶は少し下げる

  

通常会話の記憶取得

現状

記憶参照ワードがある場合のみ、記憶を取得している。

記憶参照ワード例

覚えて

記憶

前に

好き

嫌い

俺の

重要

  

問題

この方式はトークン節約には有効だが、通常会話で「覚えている感」が薄くなる。

  

改修方針

通常会話でも、軽量記憶を最大1〜2件だけ取得する。

目的

- 会話に自然な連続性を出す
- シエルが記憶を持っているように見せる
- ただし、トークン消費は増やしすぎない

  

通常会話時の取得条件

- MAX_LIGHT_MEMORIES = 2
- 関連度が一定以上の場合のみ取得する
- 低関連の記憶は取得しない
- Core Memory は必要最小限だけ使う
- Episode Memory は直近かつ関連性が高い場合のみ使う

  

記憶参照ワード時の取得

対象

以下のような発話がある場合。

前に言ったやつ

覚えてる？

俺の好きなもの

この前の話

重要なこと

  

改修方針

記憶参照ワードがある場合は、通常より多めに取得する。

- MAX_RELEVANT_MEMORIES = 5
- Long Term Memory を優先する
- Episode Memory も検索対象にする
- tag_match を強く見る
- importance を強く見る
- summary のみ渡す

  

同じ記憶の連続使用防止

問題

同じ記憶が何度も出ると、以下の問題が発生する。

- 会話が単調になる
- シエルが同じことばかり言う
- 記憶システムが不自然に見える

  

使用する情報

last_used

last_access

used_count

  

改修方針

- 使用した記憶には last_used を記録する
- 使用回数を used_count に記録する
- 直近で使った記憶はスコアを下げる
- 同じ記憶が連続して出ないようにする
- 重要記憶でも毎回出さない

  

記憶圧縮

基本方針

LLMへ渡す記憶は、原文ではなく summary を基本にする。

  

NG形式

2026年6月23日、マスターはシエルの記憶設計について相談した。

その後、トークン節約のために記憶をどう扱うべきかを話し、

tags や summary の設計について検討した。

  

推奨形式

- マスターはシエルの記憶設計で、トークン節約と覚えている感の両立を重視している

  

summary の長さ

120文字〜180文字程度

  

方針

- 原文全文を渡さない
- summary を優先する
- summary がない場合だけ本文を短縮する
- summary は120〜180文字程度に制限する
- 日付は必要な場合だけ入れる

  

古い記憶の圧縮・統合

対象

古い episode_memory。

  

処理方針

古い Episode Memory

↓

重要度判定

↓

低重要度なら削除

↓

中重要度なら圧縮

↓

高重要度なら Long Term Memory へ統合

  

方針

- 一定期間より古い Episode Memory を確認する
- importance が低いものは削除候補にする
- 類似した記憶は統合する
- 重要な記憶は Long Term Memory へ昇格する
- 統合後は元の Episode Memory を削除またはアーカイブする

  

トークン節約設定

件数制限

MAX_RELEVANT_MEMORIES = 5

MAX_LIGHT_MEMORIES = 2

  

記憶コンテキスト上限

MAX_MEMORY_CONTEXT_CHARS = 1200

  

プロンプト全体上限

MAX_TOTAL_PROMPT_CHARS = 3500

必要に応じて以下の範囲で調整する。

MAX_TOTAL_PROMPT_CHARS = 3500〜5000

  

方針

- 記憶だけで1200文字を超えないようにする
- プロンプト全体で[3500〜5000](tel:3500%E3%80%9C5000)文字程度に収める
- 直近会話履歴も無制限に入れない
- Discord履歴、環境情報、ムード情報も圧縮対象にする
- 最後に総文字数チェックを行う

  

プロンプト投入形式

推奨形式

【関連記憶】

- マスターはシエルの内部会話機能を重視している

- マスターはラファエルの機械的で論理的な口調を好む

- 現在の開発目標はNature Remo連携、PC状態監視、内部会話である

  

方針

- 箇条書きで渡す
- 記憶の本文は渡さない
- summary を渡す
- 日付は必要な場合のみ渡す
- 同じカテゴリの記憶はまとめる
- 長すぎる場合は重要度順に削る

  

プロンプト構成案

通常会話

【システム】

人格・口調・安全制御

  

【状態】

現在のムード、好奇心、会話状態

  

【関連記憶】

最大1〜2件

  

【直近会話】

必要最小限

  

【ユーザー発話】

今回の入力

  

記憶参照ワードあり

【システム】

人格・口調・安全制御

  

【状態】

現在のムード、好奇心、会話状態

  

【関連記憶】

最大5件

  

【直近会話】

必要最小限

  

【ユーザー発話】

今回の入力

  

実装順序

Phase 1：tags 導入

- 既存記憶データに tags フィールドを追加する
- 新規記憶保存時に tags を自動生成する
- keywords と tags を分離する
- タグ辞書を作成する

  

Phase 2：記憶検索ロジック改修

- 会話内容から検索用タグを抽出する
- tag_match を計算する
- 推奨スコア式を導入する
- importance、recency、novelty をスコアに反映する

  

Phase 3：通常会話の軽量記憶取得

- 通常会話でも最大2件だけ記憶を取得する
- 関連度が低い場合は取得しない
- Core Memory と Long Term Memory を優先する
- Episode Memory は直近かつ高関連の場合のみ使う

  

Phase 4：トークン管理

- MAX_MEMORY_CONTEXT_CHARS を導入する
- MAX_TOTAL_PROMPT_CHARS を導入する
- プロンプト生成の最後に総文字数を確認する
- 超過した場合は重要度の低い記憶から削る

  

Phase 5：記憶圧縮・統合

- 古い Episode Memory を定期チェックする
- 類似記憶を統合する
- 高重要度の記憶を Long Term Memory へ昇格する
- 低重要度の古い記憶を削除する

  

実装優先度

優先度：高

- tags の追加
- tag_match の導入
- 通常会話の軽量記憶取得
- 記憶コンテキスト上限の導入

  

優先度：中

- 古い Episode Memory の圧縮
- 類似記憶の統合
- used_count の追加
- last_used による連続使用防止

  

優先度：低

- 記憶のカテゴリ別表示
- ダッシュボードでの記憶確認
- 手動での記憶編集UI
- 記憶のアーカイブ機能

  

判定

現在のシエルの記憶システムは、基本設計としては有効。

ただし、以下が不足している。

- tags による分類検索
- 通常会話での軽量記憶取得
- 総トークン上限管理
- 古い記憶の圧縮・統合
- 同じ記憶の連続使用防止

  

結論

今後の設計は、以下を中核とする。

Memory Retriever

+

Memory Compressor

+

Memory Budget Manager

それぞれの役割は以下。

|   |   |
|---|---|
|要素|役割|
|Memory Retriever|会話に関係する記憶を探す|
|Memory Compressor|記憶を短く要約して渡す|
|Memory Budget Manager|トークン上限を超えないように制御する|

  

最終方針

- 記憶を削らない
- 記憶を全部読ませない
- 関連記憶だけを読む
- summary に圧縮して渡す
- tags で検索精度を上げる
- トークン上限を必ず管理する
- 少ない情報量でシエルの人格と関係性を維持する