# 2026-05-31

## 📸 現場
* 道の駅かぞわたらせ
	* <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3219.1714854405104!2d139.6600809771301!3d36.21102660068742!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x601f35b87b31b12b%3A0xd97a839285a65594!2z6YGT44Gu6aeF44GL44Ge44KP44Gf44KJ44Gb!5e0!3m2!1sja!2sjp!4v1780206134401!5m2!1sja!2sjp" width="400" height="300" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
* 

## 🧠 インプット・アイデア（Web・動画・技術）
* 場所によるが、[[SONY α7Ⅴ|α7Ⅴ]]と[[SONY α7Ⅳ|α7Ⅳ]]の2台持ちはアリ
	* 一つは腰、一つは[[Cobby カメラストラップ|ストラップ]]を使用するのが最善

## 📝 スケジュール・雑記

### Googleカレンダー予定

<%*
const events = await app.plugins.getPlugin('ics').getEvents(moment(tp.file.title, "YYYY-MM-DD"));

if (!events || events.length === 0) {
    tR += `- 予定なし\n`;
} else {
    events.sort((a, b) => a.utime - b.utime).forEach((event) => {
        const { time, endTime, summary, icsName, callUrl, callType, location, attendees } = event;

        // Extract custom fields
        const videoCallURLs = event.extractedFields?.["Video Call URLs"] || [];

        // Format attendees
        const attendeeList = attendees
            ? attendees
                .filter(attendee => attendee.name || attendee.email)
                .map(attendee => {
                    const name = attendee.name || attendee.email;
                    const email = attendee.email || "";
                    return email ? `[${name}](mailto:${email})` : name;
                })
                .join(", ")
            : "";

        // Format location
        const hasLocation = location && location.trim().length > 0;
        const encodedLocation = hasLocation ? encodeURIComponent(location.trim()) : "";

        // Calendar name
        const calendarName = icsName ? ` ${icsName}` : "";

        // Main event line
        tR += `- [ ] ${time}-${endTime} **${summary}**${calendarName}\n`;

        // Add location and Google Maps link
        if (hasLocation) {
            tR += `    - 📍 ${location}\n`;
            tR += `    - 🗺️ [Google Mapsで開く](https://www.google.com/maps/search/?api=1&query=${encodedLocation})\n`;
        }

        // Add video call URL if present
        if (callUrl) {
            tR += `    - 🎥 [${callType || "Video Call"}](${callUrl})\n`;
        }

        // Add extracted video call URLs if present
        if (videoCallURLs.length > 0) {
            tR += `    - Video Call URLs:: ${videoCallURLs.join(", ")}\n`;
        }

        // Add attendees if present
        if (attendeeList) {
            tR += `    - Attendees:: ${attendeeList}\n`;
        }
    });
}
%>

### 雑記
* le coq sportifのLCS GRAND EST CL IIを買った。届くのが楽しみ
* 今日の撮影でめちゃくちゃ日焼けした
* 

## 撮影結果

### 実際

行った場所：
道の駅 わたらせ(加須市依頼)

持参機材：
- レンズ
  - [[TAMRON 35-100mm F2.8 Di III VXD (Model A078S)]] 
  - [[TAMRON 70-180mm F／2.8 Di III VC VXD G2]] 


撮れたもの：
イベントの様子

### 振り返り

満足度：
4/5

メモ：
[[SONY α7Ⅴ|α7Ⅴ]]と[[SONY α7Ⅳ|α7Ⅳ]]の二台体制。
[[SONY α7Ⅴ|α7Ⅴ]]には[[TAMRON 35-100mm F2.8 Di III VXD (Model A078S)|35-100mm]]、[[SONY α7Ⅳ|α7Ⅳ]]には[[TAMRON 70-180mm F／2.8 Di III VC VXD G2|70-180mm]]を装着。
現像後、加須市に提出予定。