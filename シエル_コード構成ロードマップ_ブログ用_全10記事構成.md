# AIペット「シエル」を作るためのコード構成ロードマップ

この記事では、AIペット「シエル」を構成するコードを、**導入推奨順**に整理して説明します。

このロードマップは、単なる機能一覧ではありません。  
各記事の最後で必ず動作確認できるように、**1記事ごとに小さく完成する構成**へ分割しています。

---

## このロードマップの前提

シエルの完成形は、以下を統合した常駐型AIエージェントです。

- カメラ映像取得
- 人物検出
- LLM会話生成
- 音声合成
- Discord連携
- 音声入力
- 記憶管理
- 感情・興味・人格管理
- PTZ首振り制御
- Nature Remo連携
- ラファエル音声通知
- 内部会話システム

ただし、最初から全機能を入れると、どこで不具合が起きているのか判別しづらくなります。

そのため、ブログ記事として公開する場合は、以下の順番で段階的に導入する構成が適しています。

1. まず起動する
2. テキストで会話できる
3. 音声で話す
4. カメラ映像を取得する
5. 人物検出する
6. Discordから操作する
7. 音声入力で操作する
8. 記憶と感情を持たせる
9. カメラを動かす
10. 外部連携と統合運用を行う

---

# 全体の記事構成案

ブログ記事としては、**全10記事構成**が最適です。

| 記事    |    対象フェーズ | 記事テーマ                  | 記事末尾の到達状態          |
| ----- | --------: | ---------------------- | ------------------ |
| 第1記事  |   フェーズ0〜1 | 設計思想・環境構築・ログ基盤         | 起動してログが出る          |
| 第2記事  |   フェーズ2〜3 | Ollama会話AI・日本語ガード      | キーボード入力でシエルが返答する   |
| 第3記事  |   フェーズ4〜5 | GPT-SoVITS音声合成・ラファエル通知 | シエルが音声で返答する        |
| 第4記事  |     フェーズ6 | RTSPカメラ映像取得            | カメラ映像を取得できる        |
| 第5記事  |     フェーズ7 | YOLO人物検出・入退室反応         | 人物検出で入退室反応できる      |
| 第6記事  |   フェーズ8〜9 | Discord連携・遠隔制御         | Discordから会話・再起動できる |
| 第7記事  |    フェーズ10 | 音声入力・音声コマンド            | 音声で会話・簡単な操作ができる    |
| 第8記事  | フェーズ11〜13 | 記憶・感情・好奇心・回想           | 記憶と感情つきで会話できる      |
| 第9記事  | フェーズ14〜15 | PTZ首振り・内部会話            | PTZ巡回とラファエル内部会話が動く |
| 第10記事 | フェーズ16〜17 | Nature Remo連携・全機能統合    | 常駐型AIペットとして統合運用できる |

---

## 各記事の基本構成

各記事は、必ず以下の構成にします。

```markdown
# 今回作るもの
# 追加するコード
# コードの意味
# 動作確認
# よくある失敗
# 次回やること
```

この形にすると、読者は各記事の最後で「ここまで動いたか」を確認できます。

---

# フェーズ0：目的整理・必要環境・秘密情報管理

## 導入順としての位置

最初に行う工程です。  
ここではまだ本格的なコードは書かず、シエルをどのようなAIとして動かすかを整理します。

## 今回作るもの

- AIペット「シエル」の目的定義
- 必要環境の確認
- 秘密情報をコードに直書きしない方針
- ブログ公開時に伏せる情報の整理

## 使用するコードの種類

- 環境変数読み取り
- 設定値の定義
- 実行環境の確認コード

## コードの意味

シエルは、単なるチャットボットではなく、部屋に常駐して反応するAIペットです。  
そのため、最初に以下を決めておく必要があります。

- どのPCで動かすか
- Ollamaを同一PCで動かすか、別PCで動かすか
- 音声合成は GPT-SoVITS か VOICEVOX か
- カメラはRTSPで扱うか
- Discord連携を使うか
- 記憶ファイルをどこに保存するか

## 必要環境

- Windows PC
- Python
- Ollama
- OpenCV
- Ultralytics YOLO
- GPT-SoVITS または VOICEVOX
- Discord Bot
- RTSP対応カメラ
- ONVIF対応カメラ制御環境
- 任意で faster-whisper
- 任意で Nature Remo

## 注意点

以下はブログに公開してはいけません。

- Discord Bot Token
- Discord Channel ID
- Discord User ID
- RTSP URL
- カメラのユーザー名
- カメラのパスワード
- Nature Remo Access Token
- ローカルIPアドレス
- Ollama管理APIトークン
- 個人名や自宅環境が分かるファイルパス

## 動作確認

このフェーズ単体では、まだ大きな動作確認は行いません。  
ただし、以下を確認します。

- Pythonが起動できる
- 必要なライブラリをインストールできる
- `.env` または環境変数から値を読み取れる
- 秘密情報をコードに直書きしていない

---

# フェーズ1：フォルダ構成・設定ファイル・ログ基盤

## 導入順としての位置

実際に最初に実装するコードです。  
後続機能の保存先、ログ出力先、モデル配置先をここで固定します。

## 今回作るもの

- シエル用フォルダ構成
- JSON保存ファイル
- ログ出力関数
- 起動確認用の最小 `main`
- 例外発生時のログ保存

## 使用するコードの種類

- `Path` によるフォルダ定義
- `DATA_DIR`
- `ASSETS_DIR`
- `MODELS_DIR`
- `MODULES_DIR`
- `DEBUG_DIR`
- `BACKUP_DIR`
- `TEMP_DIR`
- JSON保存
- ログ出力

## コードの意味

`BASE_DIR = Path(__file__).resolve().parent` を基準にすることで、バッチファイルやショートカットから起動しても保存先がずれにくくなります。

また、最初にログ基盤を作ることで、後からTTS、Discord、カメラ、Ollamaを追加した際に原因調査がしやすくなります。

## 期待できる動作

- 必要なフォルダが自動生成される
- 起動ログが出る
- エラーログを保存できる
- JSONファイルの読み書きができる

## 注意点

- 後から保存先を変えると記憶ファイルやデバッグファイルが分散する
- `print` だけに頼らず、必ずファイルログを残す
- ブログ掲載時は実パスに含まれるユーザー名を伏せる

## 動作確認

記事末尾で以下を確認します。

- `python ai_pet_brain.py` で起動する
- `data/` や `debug/` が作成される
- ログファイルに「起動しました」と出る
- 異常終了せずに終了できる

## 第1記事の到達状態

**起動してログが出る状態。**

---

# フェーズ2：Ollamaで最小チャットAIを作る

## 導入順としての位置

フォルダとログ基盤の次に実装します。  
まずはカメラも音声も使わず、キーボード入力だけで会話できる状態を作ります。

## 今回作るもの

- Ollama API呼び出し
- テキスト入力
- シエルの返答生成
- 最小プロンプト
- フォールバック返答

## 使用するコードの種類

- HTTPリクエスト
- モデル名設定
- プロンプト生成
- 入力ループ
- 返答整形
- 失敗時の固定返答

## 主なコード

- `OLLAMA_URL`
- `CHAT_MODEL`
- `select_ollama_model`
- `ollama_generate`
- `generate_ai_message`
- `clean_ai_text`
- `fallback_chat_message`

## コードの意味

OllamaへHTTPリクエストを送り、LLMから返答を取得します。  
この時点では、記憶・感情・音声・カメラを入れず、まず「入力したら返事が返ってくる」ことだけを確認します。

## 期待できる動作

- キーボード入力でシエルと会話できる
- Ollamaの返答を取得できる
- モデル応答失敗時も無言にならない

## 注意点

- 最初からVisionモデルや記憶を混ぜない
- timeoutを短くしすぎると失敗しやすい
- `num_predict` を大きくしすぎると応答が遅くなる
- 失敗時の固定文は必ず用意する

## 動作確認

記事末尾で以下を確認します。

- キーボードで「こんにちは」と入力する
- シエルが日本語で返答する
- Ollama停止時にフォールバック文が返る
- ログに応答時間が残る

---

# フェーズ3：日本語ガード・応答品質チェック

## 導入順としての位置

最小チャットAIが動いた直後に追加します。  
AIペットでは、LLMの破綻返答をそのまま喋らせると体験が壊れます。

## 今回作るもの

- 日本語固定プロンプト
- 英語混入検出
- 分析文検出
- 不良応答判定
- 安全な返答への差し替え

## 使用するコードの種類

- プロンプト整形
- 正規表現
- 文字種判定
- NG応答判定
- フォールバック処理

## 主なコード

- `apply_japanese_dialogue_guard`
- `apply_japanese_system_guard`
- `is_likely_english_analysis_text`
- `is_likely_non_japanese_dialogue_text`
- `is_bad_chat_response`
- `clean_ai_text`

## コードの意味

Qwen系モデルなどで発生しやすい、英語の分析文や内部思考文の混入を検出し、必要に応じて返答を差し替えます。

## 期待できる動作

- 英語の分析文を返しにくくなる
- キャラクター性を維持しやすくなる
- 不自然な長文やメタ発言を抑制できる

## 注意点

- ガードを強くしすぎると返答が単調になる
- ただし常駐AIでは、自由度より破綻防止を優先する
- TTS導入前に必ずこの段階を入れる

## 動作確認

記事末尾で以下を確認します。

- 通常の日本語会話が成立する
- 英語の分析文が返ってきた場合に検出できる
- 空返答や異常返答が固定文へ差し替わる

## 第2記事の到達状態

**キーボード入力でシエルが安定して日本語返答する状態。**

---

# フェーズ4：GPT-SoVITSでシエルの音声合成を実装する

## 導入順としての位置

テキスト会話が安定してから導入します。  
ここでシエルは、画面上のAIから「部屋で声を出すAI」になります。

## 今回作るもの

- GPT-SoVITS接続
- VOICEVOXフォールバック
- TTS用テキスト正規化
- 音声分割
- WAV再生
- TTS再生状態管理

## 使用するコードの種類

- HTTPリクエスト
- Gradio WebUI呼び出し
- 音声ファイル生成
- テキスト分割
- 音量補正
- 音声再生
- 再生中フラグ管理

## 主なコード

- `TTS_ENGINE`
- `GPT_SOVITS_URL`
- `GPT_SOVITS_REF_AUDIO`
- `speak_gpt_sovits`
- `speak_voicevox_fallback`
- `speak_vox`
- `split_tts_text_for_gpt_sovits`
- `make_tts_safe_text`
- `mark_tts_playback_started`
- `mark_tts_playback_finished`

## コードの意味

LLMが生成した返答をGPT-SoVITSへ渡し、シエルの声として再生します。  
GPT-SoVITSが失敗した場合はVOICEVOXへフォールバックできるようにします。

## 期待できる動作

- シエルが音声で返答する
- 長文を分割して読み上げる
- GPT-SoVITS失敗時も無音になりにくい
- TTS中であることを他機能が判定できる

## 注意点

- GPT-SoVITSは短文欠落や繰り返しが起きる場合がある
- 句読点、分割方式、temperature、repetition penalty の調整が重要
- TTS再生中フラグを持たないと、音声入力導入時に自己ループする
- 参照音声パスは安定した場所に置く

## 動作確認

記事末尾で以下を確認します。

- キーボード入力に対してシエルが音声で返答する
- 生成されたWAVが一時フォルダに保存される
- GPT-SoVITS停止時にVOICEVOXへフォールバックする
- TTS再生開始・終了がログに残る

---

# フェーズ5：ラファエル音声通知を追加する

## 導入順としての位置

シエルの音声再生が安定した後に導入します。  
シエルの会話音声と、システム通知用のラファエル音声を分離します。

## 今回作るもの

- ラファエル用GPT-SoVITS設定
- ログ読み上げ
- 通知キュー
- 通知ワーカー
- 定期状態報告

## 使用するコードの種類

- 別ポートのGPT-SoVITS設定
- 通知キュー
- ワーカースレッド
- ログフィルタ
- ラファエル用テキスト正規化

## 主なコード

- `RAPHAEL_LOG_VOICE_ENABLED`
- `RAPHAEL_GPT_SOVITS_GRADIO_URL`
- `speak_rafael_log`
- `_enqueue_rafael_log_from_message`
- `_rafael_log_voice_worker`
- `start_rafael_log_voice_worker_thread`
- `_enqueue_rafael_periodic_status`
- `_rafael_periodic_status_worker`

## コードの意味

シエルの会話音声とは別に、起動、再起動、異常、状態報告などをラファエル音声で通知します。

## 期待できる動作

- 起動完了をラファエルが通知する
- 重要ログを音声で確認できる
- シエルの会話とシステム通知を分離できる

## 注意点

- シエル音声とラファエル音声が同時再生されないようにする
- ログをすべて読ませると騒がしくなる
- 通知頻度にはクールダウンが必要
- ラファエル用WebUIはシエル用と別ポートにする方が安定する

## 動作確認

記事末尾で以下を確認します。

- 起動時にラファエル音声が再生される
- エラーログをテスト送信するとラファエルが読み上げる
- シエルの通常会話音声と混線しない

## 第3記事の到達状態

**シエルが音声で返答し、ラファエルがシステム通知できる状態。**

---

# フェーズ6：RTSPカメラ映像を取得する

## 導入順としての位置

音声会話の基礎ができた後に導入します。  
この段階では人物検出までは行わず、カメラ映像を安定して取得することだけを目的にします。

## 今回作るもの

- RTSP接続
- OpenCV映像取得
- 別スレッドでのフレーム更新
- 最新フレーム取得
- 接続失敗時の再試行
- 画像のbase64変換
- 画面変化検出

## 使用するコードの種類

- `cv2.VideoCapture`
- スレッド処理
- フレーム保持
- base64変換
- 画像ハッシュ
- 再接続処理

## 主なコード

- `RTSP_URL`
- `VideoStreamReceiver`
- `frame_to_base64`
- `crop_monitor_focus_frame`
- `frame_to_average_hash`
- `update_visual_state`

## コードの意味

カメラ映像を別スレッドで受信し、メイン処理から最新フレームだけを取得できるようにします。  
後のVision解析やYOLO人物検出の土台になります。

## 期待できる動作

- Tapo C200などのRTSP映像を取得できる
- メインループを止めずに映像を扱える
- カメラ切断時も本体が落ちにくい
- Visionモデルへ画像を送る準備ができる

## 注意点

- RTSP URLは絶対に公開しない
- 映像取得は詰まりやすいため別スレッド化する
- カメラ切断時の再接続処理が必要
- この段階では人物検出を混ぜない

## 動作確認

記事末尾で以下を確認します。

- カメラ映像を1フレーム取得できる
- debugフォルダへ確認用画像を保存できる
- カメラを一時的に切断しても本体が落ちない
- 再接続後に再びフレームを取得できる

## 第4記事の到達状態

**RTSPカメラ映像を安定して取得できる状態。**

---

# フェーズ7：YOLO人物検出・入退室反応を実装する

## 導入順としての位置

カメラ映像取得が安定してから導入します。  
ここで初めて、シエルは「マスターが部屋にいるか」を判断できるようになります。

## 今回作るもの

- YOLOモデル読み込み
- person検出
- ROI判定
- confidence判定
- boxサイズ判定
- 入場スコア
- 離席スコア
- 入退室時の発話

## 使用するコードの種類

- Ultralytics YOLO
- bounding box判定
- ROI判定
- スコア制御
- 状態遷移
- イベント発話

## 主なコード

- `YOLO_MODEL_PATH`
- `PERSON_CONFIDENCE`
- `ENTER_SCORE_THRESHOLD`
- `LEAVE_SCORE_THRESHOLD`
- `ENTER_CONFIRM_SECONDS`
- `LEAVE_CONFIRM_SECONDS`
- `get_person_roi`
- `analyze_person_detection`
- `format_person_detection_info`

## コードの意味

カメラ映像から `person` クラスを検出します。  
単純に「人物が見えたら入場」ではなく、スコアと確認時間を使って安定判定します。

## 期待できる動作

- マスターの入場を検出する
- 離席を検出する
- 一瞬の誤検出で反応しにくくなる
- 入場時・離席時にシエルが発話する

## 注意点

- 椅子、服、影、反射をperson扱いする可能性がある
- ROIで作業席周辺に範囲を絞ると安定する
- `ENTER_CONFIRM_SECONDS` と `LEAVE_CONFIRM_SECONDS` を短くしすぎない
- PTZ導入前に、固定カメラ状態で人物検出を安定させる

## 動作確認

記事末尾で以下を確認します。

- マスターが映ると `present=True` になる
- 離席すると一定時間後に `present=False` になる
- 入場時にシエルが挨拶する
- 離席時にシエルが見送り発話する
- 椅子だけが映っている状態で誤反応しない

## 第5記事の到達状態

**人物検出で入退室反応できる状態。**

---

# フェーズ8：Discord連携を実装する

## 導入順としての位置

カメラと音声が動いた後に導入します。  
Discordを外部操作パネルとして使えるようにします。

## 今回作るもの

- Discord Bot起動
- 通常会話チャンネル
- ログチャンネル
- メッセージ受信
- 返信送信
- 認可ユーザー判定

## 使用するコードの種類

- Discord Bot
- 非同期処理
- チャンネルID管理
- ユーザー認可
- メッセージ送信キュー
- ログ転送

## 主なコード

- `DISCORD_ENABLED`
- `DISCORD_TOKEN`
- `DISCORD_CHANNEL_ID`
- `DISCORD_LOG_CHANNEL_ID`
- `DISCORD_AUTHORIZED_USER_IDS`
- `run_discord_bot`
- `start_discord_thread`
- `send_discord_reply_if_needed`
- `send_discord_event_message`
- `record_discord_exchange_if_needed`

## コードの意味

Discordからシエルに話しかけ、返答を受け取れるようにします。  
また、通常会話チャンネルとログチャンネルを分けることで、操作と監視を分離します。

## 期待できる動作

- Discordからシエルと会話できる
- 入退室イベントをDiscordへ通知できる
- ログチャンネルで内部状態を確認できる

## 注意点

- Discord Tokenは絶対に公開しない
- Bot自身のメッセージを再処理しない
- ログ転送量を制限する
- 制御コマンドは認可ユーザー限定にする

## 動作確認

記事末尾で以下を確認します。

- Discordに「こんにちは」と送る
- シエルがDiscordへ返答する
- ログチャンネルに起動ログが出る
- Bot自身の返答に再反応しない

---

# フェーズ9：停止・再起動・Ollama制御を追加する

## 導入順としての位置

Discord連携後に導入します。  
長時間運用するには、遠隔で安全に停止・再起動できる仕組みが必要です。

## 今回作るもの

- システム制御コマンド
- 認可チェック
- 安全な終了処理
- 外部再起動スケジュール
- RTSP解放
- Ollama管理API呼び出し

## 使用するコードの種類

- コマンド解析
- 認可判定
- 終了処理
- 再起動予約
- 外部プロセス起動
- APIリクエスト

## 主なコード

- `parse_system_control_command`
- `execute_discord_system_control`
- `graceful_system_cleanup`
- `schedule_external_relaunch`
- `close_runtime_video_stream`
- `parse_ollama_control_command`
- `request_ollama_manager`
- `execute_discord_ollama_control`

## コードの意味

Discordから「停止」「再起動」などを受け取り、安全に処理します。  
特にRTSP映像やワーカースレッドを閉じてから終了することで、次回起動時の不具合を減らします。

## 期待できる動作

- Discordからシエルを停止できる
- Discordから再起動できる
- カメラ接続を解放してから終了できる
- Ollama側の状態確認や制御ができる

## 注意点

- 停止・再起動は認可ユーザーだけに許可する
- 再起動は外部ランチャー経由の方が安全
- RTSPを解放しないと再起動後にカメラが詰まる場合がある
- Ollama管理APIのトークンは公開しない

## 動作確認

記事末尾で以下を確認します。

- 認可ユーザーがDiscordから再起動できる
- 非認可ユーザーの再起動要求は拒否される
- 再起動後にカメラ映像を再取得できる
- ログに終了処理と再起動処理が残る

## 第6記事の到達状態

**Discordから会話・ログ確認・再起動ができる状態。**

---

# フェーズ10：音声入力・音声コマンドを追加する

## 導入順としての位置

Discordで安全に制御できるようになった後に導入します。  
音声入力は便利ですが、誤認識リスクがあるため後半に回します。

## 今回作るもの

- 音声認識結果の受信
- ウェイクワード判定
- 音声会話状態
- 音声コマンド分類
- TTS中の入力抑制
- 音声コマンドキュー

## 使用するコードの種類

- faster-whisper連携
- 音声認識コールバック
- ウェイクワード判定
- コマンド分類
- 状態フラグ
- キュー処理

## 主なコード

- `VOICE_INPUT_ENABLED`
- `VOICE_WAKE_ENABLED`
- `VOICE_COMMAND_ENABLED`
- `VOICE_CONVERSATION_ENABLED`
- `handle_voice_input_result`
- `classify_voice_command`
- `dispatch_voice_command`
- `begin_voice_conversation`
- `finish_voice_conversation`
- `is_voice_input_blocked_by_tts`

## コードの意味

マイク入力から得たテキストを、通常会話か音声コマンドかに分類します。  
また、TTS再生中はシエル自身の声を拾わないように入力を抑制します。

## 期待できる動作

- 「シエル」と呼びかけて会話できる
- 音声で簡単な操作ができる
- 音声会話中は自律発話を抑制できる
- TTS自己ループを抑えられる

## 注意点

- 音声認識は必ず誤認識する前提で設計する
- 破壊的操作は厳密一致かつ高信頼度にする
- TTS直後の音声入力は無視する
- 最初から全音声コマンドを有効化しない

## 動作確認

記事末尾で以下を確認します。

- 「シエル」と呼ぶと反応待機状態になる
- 音声で「こんにちは」と話すと返答する
- TTS再生中に自己音声を拾って再応答しない
- 音声コマンドで簡単な操作ができる

## 第7記事の到達状態

**音声で会話・簡単な操作ができる状態。**

---

# フェーズ11：記憶システムを追加する

## 導入順としての位置

会話、音声、Discordが安定してから導入します。  
記憶はAIペットらしさの中核ですが、先に入れるとデバッグが難しくなります。

## 今回作るもの

- 短期会話履歴
- 長期記憶
- エピソード記憶
- 記憶キュー
- 記憶ワーカー
- 関連記憶検索
- 自動記憶抽出

## 使用するコードの種類

- JSON保存
- リスト管理
- 要約処理
- キュー処理
- ワーカースレッド
- 関連度検索
- プロンプトへの記憶挿入

## 主なコード

- `default_memory`
- `load_memory`
- `save_memory`
- `normalize_memory`
- `add_long_term_memory`
- `retrieve_long_term_memories`
- `format_long_term_memory_context`
- `enqueue_episode_memory`
- `process_memory_queue_item`
- `memory_worker`
- `apply_auto_long_term_memory`

## コードの意味

短期会話履歴、長期記憶、エピソード記憶を分けて保存します。  
重い記憶処理はワーカー化し、通常会話の応答速度を妨げないようにします。

## 期待できる動作

- 過去の会話を参照できる
- 「覚えて」と言われた情報を保存できる
- 重要な出来事をあとで思い出せる
- 会話に継続性が出る

## 注意点

- 記憶をプロンプトへ入れすぎると遅くなる
- 自動記憶は誤保存が起きる
- 明示記憶と自動記憶を区別する
- 記憶上限と要約処理を設ける

## 動作確認

記事末尾で以下を確認します。

- 「私の好きなものはチョコミント」と伝える
- 次の会話でその情報を参照できる
- JSONに長期記憶が保存される
- 再起動後も記憶が残っている

---

# フェーズ12：感情・好奇心・人格成長を追加する

## 導入順としての位置

記憶システムの後に導入します。  
保存された記憶や会話履歴をもとに、状態変化を持たせます。

## 今回作るもの

- ムード値
- 感情状態
- 好奇心
- 性格プロファイル
- 関係性レベル
- 時間経過による減衰
- ステータス確認

## 使用するコードの種類

- 数値状態管理
- 感情導出ロジック
- 時間減衰
- イベント加算
- プロンプト反映
- 状態サマリー

## 主なコード

- `apply_mood_event`
- `update_mood_time_decay`
- `derive_emotion_state`
- `emotion_summary`
- `mood_summary`
- `default_personality_profile`
- `normalize_personality_profile`
- `personality_profile_instruction`
- `adjust_current_curiosity`

## コードの意味

会話、入室、放置、褒め言葉、叱責などから、なつき度、信頼、寂しさ、元気、好奇心を変化させます。  
その数値から現在感情を導出し、返答に反映します。

## 期待できる動作

- 入室や会話で状態が変化する
- 放置時間で寂しさが変化する
- 状態に応じて発話が変わる
- シエルの人格に継続性が出る

## 注意点

- 加算だけではすぐ100に張り付く
- 減衰処理が必須
- 感情状態はプロンプトへ短く渡す
- 数値が複雑すぎると原因調査が難しくなる

## 動作確認

記事末尾で以下を確認します。

- 会話後に信頼やなつき度が変化する
- 放置後に寂しさが変化する
- ステータス確認で現在状態を見られる
- 状態に応じて返答の雰囲気が変わる

---

# フェーズ13：Topic Fatigue・回想イベントを追加する

## 導入順としての位置

記憶と感情が動いた後に導入します。  
自然な自律発話を作るための調整レイヤーです。

## 今回作るもの

- 話題疲労
- 話題ごとの回復
- 話題抑制プロンプト
- 回想候補抽出
- 回想発火判定
- 回想発話

## 使用するコードの種類

- 話題抽出
- スコア管理
- 時間回復
- プロンプト制御
- 記憶検索
- 確率判定

## 主なコード

- `normalize_topic_fatigue`
- `recover_topic_fatigue`
- `extract_topic_fatigue_keys`
- `apply_topic_fatigue_on_speech`
- `build_topic_fatigue_instruction`
- `collect_recollection_candidates`
- `should_start_recollection_event`
- `prepare_recollection_event`

## コードの意味

同じ話題を繰り返しすぎないように話題疲労を管理します。  
また、過去の記憶から回想イベントを起こし、シエルが記憶を持っているような発話を作ります。

## 期待できる動作

- 同じ自律発話が連続しにくくなる
- 過去の記憶をふと思い出す
- 自律発話に変化が出る
- AIペットとしての継続感が増す

## 注意点

- 回想頻度が高いと不自然になる
- 記憶が少ない初期段階では無理に発火させない
- 話題疲労を通常会話に強く適用しすぎない
- 回想発話は短くする

## 動作確認

記事末尾で以下を確認します。

- 同じ話題の自律発話が連続しない
- 保存済みの記憶から回想発話が生成される
- 回想発話がログに残る
- 回想頻度が高すぎない

## 第8記事の到達状態

**記憶・感情・好奇心・回想つきで会話できる状態。**

---

# フェーズ14：PTZ首振り・自律巡回を追加する

## 導入順としての位置

人物検出、状態管理、Discord制御が安定してから導入します。  
カメラを動かす機能は人物検出へ影響するため、後半に回します。

## 今回作るもの

- ONVIF接続
- PTZ移動
- ホーム位置保存
- 手動カメラ操作
- 自律巡回
- マスター探索
- 人物検出中の小移動
- パーソナライズ巡回

## 使用するコードの種類

- ONVIF通信
- PTZコマンド
- 状態保存
- 移動クールダウン
- 巡回スケジューラ
- 人物検出との連携

## 主なコード

- `TapoPTZController`
- `TapoPTZPatrolController`
- `parse_ptz_command`
- `execute_ptz_command`
- `decide_personalized_patrol_mode`
- `get_personalized_patrol_interval`
- `build_personalized_patrol_speech`
- `maybe_emit_personalized_patrol_speech`

## コードの意味

Tapo C200などのPTZカメラを、Discordや音声コマンドから動かします。  
また、シエルの寂しさ、好奇心、元気などに応じて巡回間隔や行動を変えます。

## 期待できる動作

- カメラを上下左右に動かせる
- ホーム位置へ戻せる
- 自律的に周囲を見回せる
- マスター不在時に探索行動ができる
- 人物検出中は小さく視線を動かせる

## 注意点

- カメラ移動中は人物検出が乱れる
- PTZ直後は離席判定を一時的に無効化する
- 外部アプリでカメラを動かすとホーム位置がずれる
- 自律移動と手動移動が衝突しないようにする

## 動作確認

記事末尾で以下を確認します。

- Discordまたは音声でカメラを左右に動かせる
- ホーム復帰できる
- 自律巡回が一定間隔で動く
- PTZ直後に誤離席判定されない

---

# フェーズ15：内部会話システムを追加する

## 導入順としての位置

ラファエル音声、記憶、感情、自律発話が揃った後に導入します。  
演出面の拡張機能なので、最初から入れるべきではありません。

## 今回作るもの

- 内部会話エンジン
- シエル状態の構築
- シエルとラファエルの掛け合い
- 内部会話履歴
- Discord内部ログ
- 音声再生振り分け

## 使用するコードの種類

- 内部会話生成
- 状態サマリー
- TTS再生制御
- 会話履歴保存
- Discordログ送信

## 主なコード

- `internal_dialogue_enabled`
- `_get_internal_dialogue_engine`
- `_build_internal_dialogue_ciel_state`
- `generate_internal_dialogue_lines_for_autonomous`
- `play_internal_dialogue_lines`
- `record_internal_dialogue_history`
- `post_internal_dialogue_lines_to_discord`

## コードの意味

自律発話の一部を、シエル単独の発話ではなく、シエルとラファエルの短い掛け合いとして生成します。

## 期待できる動作

- シエルとラファエルの内部会話が発生する
- 自律発話に変化が出る
- ラファエルが通知音声だけでなくキャラクター化する
- Discordログで内部会話を確認できる

## 注意点

- 頻度が高すぎると通常会話を邪魔する
- TTS中、会話中、人物検出不安定時は抑制する
- 1回の内部会話は短くする
- 最初は低頻度で導入する

## 動作確認

記事末尾で以下を確認します。

- 条件を満たしたときに内部会話が1回発生する
- シエル音声とラファエル音声が正しく分かれる
- Discordログに内部会話が残る
- 通常会話中には内部会話が割り込まない

## 第9記事の到達状態

**PTZ巡回とラファエル内部会話が動く状態。**

---

# フェーズ16：Nature Remo連携を追加する

## 導入順としての位置

本体機能が安定してから導入します。  
家電操作を含む外部連携なので、かなり後半に回します。

## 今回作るもの

- Nature Remo API接続
- 室温・湿度取得
- エアコン状態取得
- 状態保存
- 推奨設定生成
- エアコン操作コマンド

## 使用するコードの種類

- APIリクエスト
- 環境変数トークン
- JSON保存
- 定期ポーリング
- コマンド解析
- 安全確認フラグ

## 主なコード

- `get_nature_remo_access_token`
- `read_nature_remo_state`
- `write_nature_remo_state`
- `start_nature_remo_thread`
- `format_nature_remo_status`
- `format_nature_remo_recommendation`
- `parse_nature_remo_command`
- `execute_nature_remo_command`
- `format_nature_remo_context`

## コードの意味

Nature Remoから室温、湿度、エアコン状態を取得し、シエルの会話や提案に反映します。  
設定によっては、エアコン操作も実行できます。

## 期待できる動作

- シエルが室温や湿度を把握できる
- 環境に応じた発話ができる
- エアコン状態を確認できる
- 許可した場合のみエアコン操作できる

## 注意点

- Tokenは環境変数で管理する
- 最初は操作機能をOFFにして、状態取得だけにする
- 家電操作は誤作動防止が必須
- API失敗時もシエル本体を止めない

## 動作確認

記事末尾で以下を確認します。

- 室温と湿度を取得できる
- エアコン状態を取得できる
- シエルが環境情報を会話に反映できる
- 操作OFF時にはエアコン操作が実行されない

---

# フェーズ17：メインループで全機能を統合する

## 導入順としての位置

最後に全機能を統合します。  
ここで初めて、シエルは常駐型AIペットとして完成します。

## 今回作るもの

- 初期化処理
- 各ワーカー起動
- カメラ取得
- Discord起動
- 音声入力起動
- 記憶ワーカー起動
- 人物検出
- 会話処理
- 自律発話
- PTZ巡回
- Nature Remo処理
- 例外処理
- 終了処理

## 使用するコードの種類

- 無限ループ
- スレッド起動
- 状態管理
- 入力処理
- 例外処理
- 終了処理
- フォールバック処理

## 主なコード

- `main`

## コードの意味

すべての機能をまとめる中核です。  
RTSP、YOLO、Ollama、GPT-SoVITS、Discord、音声入力、記憶、PTZ、Nature Remoを、常駐処理として統合します。

## メインループの流れ

1. 設定を読み込む
2. ラファエル通知ワーカーを起動する
3. Nature Remoワーカーを起動する
4. YOLOモデルを読み込む
5. RTSP映像取得を開始する
6. 音声入力を開始する
7. Discord Botを開始する
8. 記憶ワーカーを開始する
9. PTZをホームへ戻す
10. 起動完了を通知する
11. 無限ループで入力・認識・発話・保存を処理する

## 期待できる動作

- シエルが常駐する
- 入退室に反応する
- Discordや音声で会話できる
- 自律発話する
- 過去を記憶する
- 状態に応じて反応が変わる
- カメラを動かす
- 室温や湿度を扱う
- ラファエルがシステム通知する

## 注意点

- 重い処理をメインループへ直接入れない
- LLM、TTS、Discord、RTSP、音声入力はワーカー化する
- 例外で落ちないようにする
- どの機能が失敗しても本体が継続する設計にする
- 常駐運用では成功時の処理より失敗時の処理が重要

## 動作確認

記事末尾で以下を確認します。

- 起動後にラファエルが起動通知する
- カメラ映像を取得する
- 人物検出が動く
- Discordから会話できる
- 音声で会話できる
- TTSが自己ループしない
- 記憶が保存される
- PTZ巡回が動く
- Nature Remo情報を取得できる
- エラーが起きても本体が即停止しない

## 第10記事の到達状態

**常駐型AIペットとして統合運用できる状態。**

---

# 全10記事の最終まとめ

## 第1記事：AIペット「シエル」の設計思想と起動基盤

対象フェーズ：

- フェーズ0
- フェーズ1

到達状態：

- 起動する
- ログが出る
- 必要フォルダが作成される
- 秘密情報を直書きしない構成になる

---

## 第2記事：Ollamaでシエルの会話AIを作る

対象フェーズ：

- フェーズ2
- フェーズ3

到達状態：

- キーボード入力で会話できる
- 日本語返答が安定する
- 異常返答を検出できる

---

## 第3記事：GPT-SoVITSでシエルに声を与える

対象フェーズ：

- フェーズ4
- フェーズ5

到達状態：

- シエルが音声で返答する
- ラファエルがシステム通知する
- TTS再生状態を管理できる

---

## 第4記事：RTSPカメラ映像を取得する

対象フェーズ：

- フェーズ6

到達状態：

- カメラ映像を取得できる
- 確認用フレームを保存できる
- カメラ切断時も本体が落ちにくい

---

## 第5記事：YOLOでマスターを認識して入退室反応する

対象フェーズ：

- フェーズ7

到達状態：

- 人物検出できる
- 入場反応できる
- 離席反応できる
- 誤検出をある程度抑制できる

---

## 第6記事：Discordからシエルを操作する

対象フェーズ：

- フェーズ8
- フェーズ9

到達状態：

- Discordから会話できる
- ログをDiscordで確認できる
- 認可ユーザーだけが再起動できる

---

## 第7記事：音声入力でシエルと会話する

対象フェーズ：

- フェーズ10

到達状態：

- 音声で会話できる
- 音声コマンドを処理できる
- TTS自己ループを抑制できる

---

## 第8記事：記憶・感情・好奇心でAIペットらしさを作る

対象フェーズ：

- フェーズ11
- フェーズ12
- フェーズ13

到達状態：

- 記憶を保存できる
- 感情状態が変化する
- 好奇心や話題疲労が動く
- 回想発話が発生する

---

## 第9記事：Tapo C200の首振りとラファエル内部会話を実装する

対象フェーズ：

- フェーズ14
- フェーズ15

到達状態：

- PTZ操作できる
- 自律巡回できる
- シエルとラファエルの内部会話が発生する

---

## 第10記事：Nature Remo連携と全機能の統合

対象フェーズ：

- フェーズ16
- フェーズ17

到達状態：

- 室温・湿度を取得できる
- エアコン状態を確認できる
- 全機能をメインループで統合できる
- 常駐型AIペットとして運用できる

---

# 最終的な導入順

実装順だけを簡潔にまとめると、以下です。

1. 目的・必要環境・秘密情報管理を決める
2. フォルダ構成とログ基盤を作る
3. Ollamaで最小チャットAIを作る
4. 日本語ガードと異常返答チェックを入れる
5. GPT-SoVITSで音声を出す
6. ラファエル通知を追加する
7. RTSPカメラ映像を取得する
8. YOLOで人物検出する
9. Discordで会話・ログ確認できるようにする
10. Discordから停止・再起動できるようにする
11. 音声入力と音声コマンドを追加する
12. 記憶システムを追加する
13. 感情・好奇心・人格成長を追加する
14. 話題疲労と回想イベントを追加する
15. PTZ首振りと自律巡回を追加する
16. シエルとラファエルの内部会話を追加する
17. Nature Remoで環境理解を追加する
18. メインループで全機能を統合する

---

# 結論

シエルのコードをブログ化する場合、最適な構成は **全10記事** です。

重要なのは、完成形の機能分類ではなく、**1記事ごとに動作確認できる導入順**で分けることです。

前回のように全8記事構成にすると、以下の問題が発生します。

- カメラ取得と人物検出が同じ記事に入って重い
- Discord制御と音声入力が同じ記事に入って重い
- 記憶、感情、回想が同じ記事に詰まりすぎる
- PTZと内部会話を同時に入れると不具合原因が追いづらい
- Nature Remoと統合運用を同時に扱うと検証項目が多すぎる

そのため、各記事の到達状態を明確にし、以下の順番で進めるのが最適です。

1. 起動する
2. 返答する
3. 喋る
4. 映像を見る
5. 人物を認識する
6. Discordで操作する
7. 音声で操作する
8. 記憶と感情を持つ
9. カメラを動かし、内部会話する
10. 外部環境と統合する

この構成なら、読者は各記事ごとに「ここまで動いたか」を確認できます。  
結果として、トラブルシューティングしやすく、ブログ連載としても読みやすい構成になります。
