---
type: サブスクリプション-index
作成日: 2026-06-06
---

# サブスク集計

## 月額換算一覧

```dataview
TABLE
  プラン,
  カテゴリ,
  支払方法,
  月額換算,
  年額換算,
  契約状況
FROM "サブスクリプション"
WHERE type = "サブスクリプション"
SORT 月額換算 DESC
```

## 月額合計

```dataview
TABLE WITHOUT ID
  "月額合計" AS 項目,
  round(sum(rows.月額換算), 0) AS 金額
FROM "サブスクリプション"
WHERE type = "サブスクリプション" AND 契約状況 = "active"
GROUP BY true
```

## 年額合計

```dataview
TABLE WITHOUT ID
  "年額合計" AS 項目,
  round(sum(rows.年額換算), 0) AS 金額
FROM "サブスクリプション"
WHERE type = "サブスクリプション" AND 契約状況 = "active"
GROUP BY true
```

## カテゴリ別の月額合計

```dataview
TABLE WITHOUT ID
  カテゴリ,
  round(sum(rows.月額換算), 0) AS 月額合計,
  round(sum(rows.年額換算), 0) AS 年額合計
FROM "サブスクリプション"
WHERE type = "サブスクリプション" AND 契約状況 = "active"
GROUP BY カテゴリ
SORT 月額合計 DESC
```

## 注意

- ChatGPT Plusは日本円固定ではなく、実請求額が為替で変動します。
- Adobe Creative Cloudは実際の年一括支払い額、特にAmazonセール購入額と異なる可能性があります。
- 実請求額が判明したら、各 `.md` ファイルの `月額換算` と `年額換算` を修正してください。
