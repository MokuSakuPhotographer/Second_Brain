
## ~~Phase 0：全体方針~~

- [x] Ollama移行前に、音声入力用ファイルの器だけ先に分離する
- [x] brain本体の大規模分割はまだ行わない
- [x] 分割対象は音声入力系だけに限定する
- [x] 音声認識の本実装はOllama移行後に行う
- [x] 音声認識はサブPC側で処理する
- [x] OllamaはメインPC側で対話生成・画像判別を担当する
- [x] GPT-SoVITS、カメラ制御、状態管理はサブPC側に残す
- [x] 既存のYOLO・RTSP・Discord・PTZ設定は極力変更しない
- [x] 既存のGPT-SoVITS設定は変更しない

---

## ~~Phase 1：現行brainの安定版保存~~

- [x] 現行ファイルを安定版としてバックアップする
- [x] バックアップ名を付ける
  - [x] 例：`ai_pet_brain_v104_stable_before_voice_split.py`
- [x] 現在の起動手順で正常起動することを確認する
- [x] Discord会話が動作することを確認する
- [x] 自律発話が動作することを確認する
- [x] GPT-SoVITS再生が動作することを確認する
- [x] PTZ制御が動作することを確認する
- [x] 入場・離席処理が動作することを確認する
- [x] モニター注視型自律発話が動作することを確認する
- [x] 回想イベントが動作することを確認する
- [x] 長期記憶・出来事記憶の保存が動作することを確認する

---

## ~~Phase 2：音声入力ファイルの器だけ分離~~

- [x] `ai_pet_voice_input.py` を新規作成する
- [x] まだfaster-whisperは実装しない
- [x] まだマイク監視は開始しない
- [x] 空の起動関数だけ用意する
  - [x] `start_voice_input_thread()`
- [x] 停止用関数だけ用意する
  - [x] `stop_voice_input_thread()`
- [x] 状態確認用関数だけ用意する
  - [x] `get_voice_input_status()`
- [x] 音声入力モジュール単体でimportできることを確認する
- [x] 音声入力モジュール側に設定領域を用意する
- [x] この段階では実際の音声認識処理は入れない

---

## ~~Phase 3：brain側に音声入力接続口を追加~~

- [x] brain側で `ai_pet_voice_input.py` をimportできるようにする
- [x] import失敗時もbrain本体が落ちないようにする
- [x] 音声入力機能のON/OFF設定を追加する
	- [x] `VOICE_INPUT_ENABLED = False`
- [x] 起動時に `VOICE_INPUT_ENABLED` を確認する
- [x] `VOICE_INPUT_ENABLED = False` の場合は何も起動しない
- [x] `VOICE_INPUT_ENABLED = True` の場合だけ音声入力スレッドを開始する設計にする
- [x] 音声入力結果を `enqueue_chat()` に渡せる設計にする
- [x] 入力元を `source="voice"` として扱えるようにする
- [x] 発話者を `author="mic"` として扱えるようにする
- [x] Discord入力と音声入力を区別できるようにする
- [x] 音声入力用ログ記号を検討する
	- [x] 例：`《音》マスター音声入力：「...」`

---

## ~~Phase 4：TTS再生中ミュート制御の準備~~

- [x] brain側に「現在シエルが話しているか」を示す状態を用意する
  - [x] 例：`is_tts_speaking`
- [x] GPT-SoVITS再生開始時に `is_tts_speaking=True` にする設計を確認する
- [x] GPT-SoVITS再生終了時に `is_tts_speaking=False` にする設計を確認する
- [x] VOICEVOXフォールバック再生時も同じ状態を使う設計にする
- [x] 音声入力側がこの状態を参照できる設計にする
- [x] TTS再生中は音声認識しない設計にする
- [x] TTS終了後も一定時間は入力を無視する設計にする
- [x] シエル自身の声をマイクが拾う無限会話対策を設計に含める

---

## ~~Phase 5：分割後の既存動作確認~~

- [x] `VOICE_INPUT_ENABLED = False` のまま起動する
- [x] 起動時にimportエラーが出ないことを確認する
- [x] 音声入力スレッドが起動していないことを確認する
- [x] Discord会話が従来通り動くことを確認する
- [x] 自律発話が従来通り動くことを確認する
- [x] GPT-SoVITS再生が従来通り動くことを確認する
- [x] PTZ制御が従来通り動くことを確認する
- [x] RTSP映像取得が従来通り動くことを確認する
- [x] 入場・離席判定が従来通り動くことを確認する
- [x] ログ量が増えすぎていないことを確認する
- [x] 問題があればPhase 1の安定版へ戻せることを確認する

---

## ~~Phase 6：Ollama移行前の現状確認~~

- [x] サブPCで現在のVRAM使用量を記録する
	-  アイドル時：9.4/10.0 GB
	- 発話動作時：9.5/10.0GB
- [x] サブPCで現在のOllama使用モデルを確認する
	- 画像推論モデル：qwen2.5vl:7b
	- テキスト推論モデル：qwen3:8b
- [x] サブPCで現在の通常会話応答速度を記録する
- [x] サブPCで現在の自律発話生成時間を記録する
- [x] サブPCで現在の画像判別待ち時間を記録する
- [x] メインPCの固定IPまたはDHCP予約を設定する
- [x] メインPCのGPUがRTX4090として認識されていることを確認する
- [x] メインPCがスリープしない設定になっていることを確認する

---

## Phase 7：メインPC側Ollama準備

- [x] メインPCにOllamaをインストールする
- [x] メインPCでOllamaを起動する
- [x] `OLLAMA_HOST=0.0.0.0:11434` を設定する
- [x] メインPCのOllamaを再起動する
- [x] WindowsファイアウォールでTCP 11434番を許可する
- [x] メインPC上でOllama APIが応答することを確認する
- [x] メインPC上で `ollama list` が動作することを確認する

---

## ~~Phase 8：メインPC側モデル導入~~

- [x] 画像判別モデルを導入する
  - [x] 初期候補：`qwen3-vl:8b`
  - [ ] 安定しない場合：`qwen2.5vl:7b`
- [x] 対話モデルを導入する
  - [x] 初期候補：`qwen3:14b`
  - [ ] 重い場合：`qwen3:8b`
  - [ ] 品質検証候補：`qwen3:32b`
- [x] `ollama list` でモデル導入を確認する
- [x] メインPC上で対話モデルの単体応答を確認する
- [x] メインPC上で画像判別モデルの単体応答を確認する
- [x] メインPC側で `nvidia-smi` を実行する
- [x] OllamaモデルがGPUを使用していることを確認する

---

## ~~Phase 9：サブPCからメインPC Ollamaへの接続確認~~

- [x] サブPCからメインPCのOllamaへ疎通確認する
- [x] 以下のURLへアクセスできることを確認する
  - [x] `http://メインPCのIP:11434/api/tags`
- [x] `curl http://メインPCのIP:11434/api/tags` を実行する
- [x] モデル一覧が返ることを確認する
- [x] サブPCから対話モデルへテスト送信する
- [x] サブPCから画像判別モデルへテスト送信する
- [x] 応答が返るまでの時間を記録する
- [x] 接続失敗時のエラー内容を記録する

---

## ~~Phase 10：シエル側Ollama設定変更~~

- [x] brain側の `OLLAMA_URL` をメインPCのIPへ変更する
  - [x] 例：`OLLAMA_URL = "http://192.168.0.xxx:11434"`
- [x] `VISION_MODEL` を新しい画像判別モデルへ変更する
  - [x] 例：`VISION_MODEL = "qwen3-vl:8b"`
- [x] `CHAT_MODEL` を新しい対話モデルへ変更する
  - [x] 例：`CHAT_MODEL = "qwen3:14b"`
- [x] `stream: false` を維持する
- [x] `keep_alive` を維持または調整する
  - [x] 例：`OLLAMA_KEEP_ALIVE = "30m"`
- [x] 既存のGPT-SoVITS設定は変更しない
- [x] 既存のYOLO設定は変更しない
- [x] 既存のRTSP設定は変更しない
- [x] 既存のDiscord設定は変更しない
- [x] 既存のPTZ設定は変更しない

---

## Phase 11：Ollama移行後の動作確認

- [x] シエル本体を起動する
- [x] Discord通常会話が動作することを確認する
- [x] 自律発話が動作することを確認する
- [x] モニター注視型自律発話が動作することを確認する
- [x] 入場・離席処理が動作することを確認する
- [x] 回想イベントが動作することを確認する
- [x] 長期記憶の保存が動作することを確認する
- [x] 出来事記憶の保存が動作することを確認する
- [x] 画像判別が正常に動くことを確認する
- [x] Ollama接続失敗時のログを確認する
- [x] エラー時にVOICEVOXまたは保険文へ逃げることを確認する
- [x] timeout時にシエル本体が停止しないことを確認する

---

## Phase 12：Ollama移行後の負荷確認

- [x] サブPCで `nvidia-smi` を実行する
- [x] サブPCのVRAM使用量が移行前より低下していることを確認する
- [x] メインPCで `nvidia-smi` を実行する
- [x] メインPC側でOllamaモデルがGPUを使用していることを確認する
- [x] 通常会話時の応答速度を確認する
	-  LLM処理全体時間: type=通常会話, elapsed=19.57s, image_prepare=0.00s, result=success
- [x] 自律発話時の生成時間を確認する
	-  《計》LLM処理全体時間: type=画像付き自律発話, elapsed=13.51s, image_prepare=0.01s, result=success
- [x] 画像判別時の待ち時間を確認する
	-  LLM処理全体時間: type=画像判別, elapsed=12.93s, image_prepare=0.00s, result=success
- [x] サブPC側でGPT-SoVITS生成が安定していることを確認する
- [x] サブPC側でYOLO人物検出が安定していることを確認する
- [x] サブPC側でRTSP映像取得が安定していることを確認する

---

## Phase 13：Ollama移行後の安定化

- [ ] メインPCが未起動の場合の挙動を確認する
- [ ] メインPCがスリープしている場合の挙動を確認する
- [ ] Ollama接続失敗時のログを確認する
- [x] 必要ならサブPC側Ollamaへ戻すフォールバックを実装する
- [x] タイムアウト秒数を調整する
- [x] モデルが重すぎる場合は一段階下げる
	 - `qwen3-vl:8b`
	 - `qwen2.5vl:7b`
	 - `qwen3:14b`
	 - `qwen3:8b`
- [x] Discord会話の体感遅延を確認する
- [x] 自律発話の生成遅延を確認する
- [x] 画像判別のタイムアウト率を確認する
- [ ] 長時間稼働テストを行う

---

## Phase 14：音声認識単体テスト開始

- [x] `VOICE_INPUT_ENABLED = True` に変更する
- [x] サブPCにUSBマイクを接続する
- [x] Windows側で入力デバイスを固定する
- [x] マイク入力取得ライブラリを導入する
- [x] マイク音量をログ出力する
- [x] 無音判定を実装する
- [x] 音声チャンクを一時保存する
- [x] TTS再生中はマイク入力を無視する
- [x] TTS終了後、一定時間マイク入力を無視する
- [x] マイク入力だけで既存機能が重くならないことを確認する

---

## Phase 15：faster-whisper導入

- [x] `faster-whisper` を導入する
	- [x] `small` または `medium` モデルを導入する
- [x] 日本語認識を固定する
- [x] 保存した音声チャンクを文字起こしする
- [x] 認識結果をログに出す
- [x] 短すぎる認識結果を破棄する
- [x] 信頼度の低い認識結果を破棄する
- [x] 認識処理中フラグを追加する
- [x] faster-whisper処理中にTTSやPTZが詰まらないことを確認する

---

## ~~Phase 16：名前呼び反応~~

- [x] 「シエル」検出処理を追加する
- [x] 「しえる」を補正候補に登録する
- [x] 「シェル」を補正候補に登録する
- [x] 「消える」を補正候補に登録する
- [x] 「CL」を補正候補に登録する
- [x] 名前検出時に即時応答する
- [x] `voice_waiting` 状態を追加する
- [x] 呼びかけ後、5〜10秒だけ音声入力を優先受付する
- [x] 待機時間経過後は通常状態へ戻す
- [x] 自律発話中は名前呼び反応の優先度を上げる
- [x] TTS再生中の誤反応を防止する

---

## ~~Phase 17：音声コマンド実装~~

- [x] 「止まって」を検出する
- [x] 「起動して」を検出する
- [x] 「再起動して」を検出する
- [x] 「こっち向いて」を検出する
- [x] 「巡回して」を検出する
- [x] 「静かにして」を検出する
- [x] 「話して」を検出する
- [x] 音声コマンドはOllamaを通さず定型処理する
- [x] 音声コマンド実行ログを追加する
- [x] 誤認識時は実行せず破棄する
- [x] 音声コマンドとDiscordコマンドの衝突を確認する

---

## Phase 18：音声会話実装

- [x] 音声入力をテキストとして会話処理へ渡す
- [x] 音声入力とDiscord入力を分離する
- [x] `source=voice` を会話ログに残す
- [x] 音声会話中は自律発話を停止する
- [x] 音声会話中はカメラ反応の優先度を下げる
- [x] 音声入力をOllamaへ送信する
- [x] Ollama応答をGPT-SoVITSへ渡す
- [x] 音声応答を再生する
- [x] timeout時の定型応答を追加する
- [ ] ~~Ollama失敗時のフォールバック応答を追加する~~
	- 実装しない
- [x] 音声会話後に通常状態へ戻ることを確認する

---

## Phase 19：感情認識拡張【保留】

- [ ] 音量解析を追加する
- [ ] 話速解析を追加する
- [ ] 音声の間の長さを解析する
- [ ] 音声感情を一時状態として保存する
- [ ] `temporary_voice_emotion` を追加する
- [ ] moodへ直接強反映しない設計にする
- [ ] 必要時のみmoodへ微量反映する
- [ ] 感情推定ログを簡潔に出力する
- [ ] 感情推定が自律発話に過剰影響しないことを確認する

---

## Phase 20：割り込み処理【保留】

- [ ] 音声優先モードを追加する
- [ ] 「止まって」を最優先コマンドにする
- [ ] TTS再生中断処理を追加する
- [ ] PTZ停止処理を追加する
- [ ] Ollama応答待機中の破棄処理を追加する
- [ ] 自律発話中の割り込みを許可する
- [ ] Discord会話中の音声割り込みルールを決める
- [ ] 割り込み後の状態復帰処理を追加する
- [ ] 割り込み後にキューが詰まらないことを確認する

---

## Phase 21：音声認識の安定化・誤爆対策【保留】

- [ ] シエル自身の声をマイクが拾う問題を確認する
- [ ] TTS中の音声認識停止を徹底する
- [ ] 同一内容の短時間再入力を破棄する
- [ ] 独り言への誤反応を確認する
- [ ] 名前呼びなしの会話開始を禁止する
- [ ] 音声待機時間を調整する
- [ ] 誤認識補正辞書を調整する
- [ ] ログ量を抑制する
- [ ] 長時間稼働テストを行う
- [ ] マイク未接続時にbrain本体が落ちないことを確認する

---

## Phase 22：ラファエル音声拡張の再検討

- [ ] Ollama移行後のサブPC VRAM空き容量を確認する
- [ ] GPT-SoVITS 2インスタンス起動の再検証を行う
- [ ] シエル用GPT-SoVITS単体のVRAM使用量を記録する
- [ ] ラファエル用GPT-SoVITS追加後のVRAM使用量を記録する
- [ ] 片方ずつ音声生成した場合の最大VRAM使用量を確認する
- [ ] 9GB以下で安定する場合のみ2インスタンス方式を採用する
- [ ] 9GBを超える場合は参照音声切替方式を採用する
- [ ] 2インスタンス方式でTTS遅延が増えないか確認する
- [ ] 参照音声切替方式で声質切替が安定するか確認する

---

## Phase 23：最終構成確定

- [ ] 安定構成を記録する
- [ ] 採用モデルを記録する
- [ ] メインPC側Ollama設定を記録する
- [ ] サブPC側シエル設定を記録する
- [ ] 起動手順を更新する
- [ ] 停止手順を更新する
- [ ] 再起動手順を更新する
- [ ] トラブル時の復旧手順を作成する
- [ ] Ollama接続不能時の復旧手順を作成する
- [ ] マイク認識不良時の復旧手順を作成する
- [ ] GPT-SoVITS不調時の復旧手順を作成する
- [ ] Obsidian用の運用メモに反映する

---

# 実装順序まとめ

- [ ] 現行brainを安定版としてバックアップする
- [ ] 音声入力用ファイルの器だけ先に分離する
- [ ] `VOICE_INPUT_ENABLED = False` のまま既存機能を確認する
- [ ] サブPCとメインPCの現状負荷を記録する
- [ ] メインPCにOllamaを導入する
- [ ] メインPCに対話モデルと画像判別モデルを導入する
- [ ] サブPCからメインPC Ollamaへの疎通を確認する
- [ ] シエル側の `OLLAMA_URL` / `VISION_MODEL` / `CHAT_MODEL` を変更する
- [ ] Discord会話・自律発話・画像判別の正常動作を確認する
- [ ] Ollama移行後の負荷と安定性を確認する
- [ ] `VOICE_INPUT_ENABLED = True` に変更する
- [ ] マイク入力と無音判定を実装する
- [ ] faster-whisperを導入する
- [ ] 「シエル」呼びかけ検出を実装する
- [ ] 音声コマンドを実装する
- [ ] 音声会話を実装する
- [ ] 感情認識を拡張する
- [ ] 割り込み処理を実装する
- [ ] 誤爆対策と長時間稼働テストを行う
- [ ] ラファエル音声拡張を再検討する
- [ ] 最終構成と運用手順をObsidianへ反映する

---

# 現時点で実装しない項目

- [ ] 常時音声会話
- [ ] 名前呼びなしの自由会話
- [ ] faster-whisper large-v3常駐
- [ ] 音声感情のmood強反映
- [ ] 完全な双方向割り込み会話
- [ ] 音声入力をすべてOllamaへ送信する設計
- [ ] brain本体の全面的なモジュール分割
- [ ] GPT-SoVITS 2インスタンスの即時採用